[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
addopts = -ra --strict-markers
markers =
    unit: fast isolated tests with no external services
    integration: tests that exercise multiple local components
    system: end-to-end or OS-adjacent tests
    experimental: non-blocking exploratory tests outside the stable CI gate
